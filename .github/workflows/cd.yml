name: CD
on:
    push:
        branches:
            - master
jobs:
    cd:
        runs-on: ${{ matrix.os }}

        strategy:
            matrix:
                os: [ubuntu-latest]
                node: [14]

        steps:
            - name: Install dependencies
              run: npm i

            - name: Build
              run: npm run deploy
